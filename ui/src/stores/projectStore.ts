/**
 * Backend workspace id for boards, pipeline stages, and integration APIs.
 * The UI no longer exposes project switching; tasks use per-task folders and boards
 * organize columns — everything uses this single API scope.
 */
export const WORKSPACE_PROJECT_ID = "default";
